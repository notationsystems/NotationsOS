[package]
name = "notations-state-kernel"
version = "0.1.0"
edition = "2021"
publish = false
description = "Deterministic, bounded notation-state replay kernel"

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"

[profile.release]
strip = true
